{
  "name": "pi-claude-plugin-manager",
  "version": "0.6.1",
  "private": true,
  "description": "Standalone Claude Code marketplace plugin lifecycle manager for Pi.",
  "keywords": [
    "pi-package",
    "pi-extension",
    "claude-code",
    "plugins"
  ],
  "scripts": {
    "smoke": "./tests/smoke.sh"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.80.0",
    "@earendil-works/pi-tui": ">=0.80.0"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
